# knowledge.md — 踩坑根因记忆库

> 历史根因库。**优先固化、其次记文档**：能变成测试断言的进 `tests/`，能变成静态检查的进 `scripts/check-conventions.mjs`（`npm run lint:conventions`），能写代码注释就近写注释；只有这三层都覆盖不了的，才记在本文件。
>
> 已固化 / 已失效项（不在本文件重复）：
> - `@keyframes` 未集中 → `scripts/check-conventions.mjs` 拦截
> - tsc 编译产物混入 src → `scripts/check-conventions.mjs` 拦截
> - 任务文件越权勾选 → 随多 agent 接力流程退役一并取消（不再共享可写的任务文件）

## K-001 CSS 块未闭合 / 悬空属性

- 为何无法固化：vite/PostCSS 对 CSS 语法宽容，缺 `}` 常常 build 不报错； brace 计数会被注释/字符串/媒体查询误判，静态检查收益不抵噪声
- 指纹：手写编辑 Vue SFC `<style>` 或原生 CSS
- 症状：缺 `}` 导致样式整体或部分失效
- 修法：编辑后逐对核对花括号；build 通过之外再目视确认样式生效
- 来源：`cb8f175`（.start-btn:hover 缺闭合 brace）

## K-002 联机模式本地重置不广播

- 为何无法固化：涉及"改变对局共享状态"的业务语义判断，静态检查无法识别哪些状态是共享的
- 指纹：联机对局中存在「重置 / 重开 / 重启」类入口
- 症状：本端本地重置，对端不知情 → 双端失同步，重置端可能永不结算
- 修法：凡是改变对局共享状态的动作，要么走房间广播 + 确认，要么直接隐藏该入口
- 来源：竞速 P0（`25673e7` 修复，竞速模式隐藏重启按钮）

## K-003 递归 setTimeout 链无取消 / 无卸载清理

- 为何无法固化：递归计时是否被正确清理依赖控制流分析，grep 无法可靠判定
- 指纹：倒计时、延迟状态机等递归计时逻辑
- 症状：重置或离开页面后残余计时器继续跑，把界面重新拉回对局
- 修法：保存 timer id，reset 与 onUnmounted 双路清理并复位状态
- 来源：竞速 P1（`25673e7` 修复 runCountdown）

## K-004 通用 composable 硬编码游戏专有配置

- 为何无法固化：是否"游戏专有"取决于语义，静态检查会把字面量误报
- 指纹：新建「通用」composable 且内部调用 useRealtimeRoom / 传 meta
- 症状：meta 里写死具体游戏名（如 `whackamole-race`），复用即失真
- 修法：游戏专有配置一律入参化，composable 给默认值
- 来源：竞速 P2（`25673e7` game 标签入参化）
